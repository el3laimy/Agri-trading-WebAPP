# نظام المحاسبة الزراعية (Agricultural Accounting System)

نظام متكامل لإدارة الحسابات الزراعية، يجمع بين واجهة مستخدم حديثة وسهلة الاستخدام ونظام خلفي قوي لإدارة البيانات المالية والمخزون.

## 📋 نظرة عامة

يهدف هذا النظام إلى مساعدة المزارعين والشركات الزراعية في:
- إدارة دورات المحاصيل (شراء، بيع، مخزون).
- تتبع المصروفات والإيرادات بدقة.
- إصدار الفواتير وتقارير الأداء المالي.
- إدارة الأصول الثابتة وحساب الإهلاك.
- استخراج تقارير محاسبية شاملة (قائمة الدخل، الميزانية العمومية، التدفقات النقدية).

---

## 🛠️ المتطلبات الأساسية (Prerequisites)

لتشغيل المشروع، تحتاج إلى تثبيت البرمجيات التالية على جهازك:

### 1. طريقة Docker (موصى بها - الأسهل)
- **Docker**: [تحميل Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Docker Compose**: يأتي عادةً مدمجاً مع Docker Desktop.

### 2. التشغيل اليدوي (للمطورين)
- **Python**: الإصدار 3.10 أو أحدث.
- **Node.js**: الإصدار 18 أو أحدث.
- **Git**: لإدارة النسخ (اختياري).

---

## 🚀 طريقة التشغيل (باستخدام Docker)

هذه الطريقة تقوم بإعداد وتشغيل قاعدة البيانات، السيرفر (Backend)، والواجهة (Frontend) بأمر واحد.

1. **نسخ المشروع:**
   ```bash
   git clone <repository_url>
   cd agricultural-accounting
   ```

2. **بناء وتشغيل الحاويات:**
   ```bash
   docker-compose up --build
   ```
   *انتظر بضع دقائق حتى يتم تحميل الصور وتثبيت المكتبات.*

3. **الوصول للتطبيق:**
   - **الواجهة الأمامية (Web App):** افتح المتصفح على [http://localhost:3000](http://localhost:3000)
   - **واجهة التوثيق (API Docs):** [http://localhost:8000/docs](http://localhost:8000/docs)

4. **إيقاف التطبيق:**
   اضغط `Ctrl + C` في الطرفية (Terminal)، أو استخدم الأمر:
   ```bash
   docker-compose down
   ```

---

## 💻 طريقة التشغيل اليدوي (Manual Setup)

إذا كنت تفضل تشغيل كل جزء على حدة للتطوير:

### 1. إعداد الخلفية (Backend)

1. اذهب لمجلد الخلفية:
   ```bash
   cd backend
   ```

2. أنشئ بيئة افتراضية (Virtual Environment):
   ```bash
   python -m venv venv
   # لنظام Windows
   venv\Scripts\activate
   # لنظام Mac/Linux
   source venv/bin/activate
   ```

3. ثبّت المكتبات المطلوبة:
   ```bash
   pip install -r requirements.txt
   ```

4. قم بتهيئة قاعدة البيانات (Migrations):
   ```bash
   # تأكد من أنك داخل مجلد backend
   python -m alembic upgrade head
   ```

5. شغّل السيرفر:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```
   سيعمل السيرفر الآن على المنفذ 8000.

### 2. إعداد الواجهة الأمامية (Frontend)

1. افتح نافذة طرفية جديدة واذهب لمجلد الواجهة:
   ```bash
   cd frontend
   ```

2. ثبّت مكتبات Node.js:
   ```bash
   npm install
   ```

3. شغّل تطبيق الواجهة:
   ```bash
   npm run dev
   ```
   سيعمل التطبيق عادةً على الرابط الذي يظهره Vite (غالباً [http://localhost:5173](http://localhost:5173) أو 3000 حسب الإعدادات).

---

## 🧪 تشغيل الاختبارات (Testing)

لضمان سلامة الكود والحسابات المالية:

### اختبارات الخلفية (Backend Tests)
```bash
cd backend
# تفعيل البيئة الافتراضية أولاً
export PYTHONPATH=$PYTHONPATH:.
pytest
```
*يغطي هذا اختبارات الدورة المستندية، حسابات الأرباح، وتكامل البيانات.*

### اختبارات الواجهة (Frontend Tests)
```bash
cd frontend
npm test
```
*يستخدم Vitest لاختبار المكونات الأساسية.*

---

## 📂 هيكلية المشروع

```
.
├── backend/                # كود السيرفر (FastAPI)
│   ├── app/
│   │   ├── api/            # نقاط الاتصال (Endpoints)
│   │   ├── core/           # الإعدادات الأساسية
│   │   ├── models.py       # نماذج قاعدة البيانات
│   │   ├── schemas.py      # نماذج التحقق (Pydantic)
│   │   └── services/       # منطق العمليات والحسابات
│   ├── tests/              # اختبارات التكامل والوحدات
│   └── alembic/            # ملفات تهجير قاعدة البيانات
│
├── frontend/               # كود الواجهة (React + Vite)
│   ├── src/
│   │   ├── components/     # المكونات القابلة لإعادة الاستخدام
│   │   ├── pages/          # صفحات التطبيق
│   │   ├── context/        # إدارة الحالة (State Management)
│   │   └── api/            # دوال الاتصال بالسيرفر
│   └── public/
│
├── docker-compose.yml      # ملف إعداد Docker
└── README.md               # ملف التوثيق الحالي
```

---

## 🔑 تسجيل الدخول الافتراضي

عند تشغيل النظام لأول مرة، سيتم إنشاء مستخدم مسؤول تلقائياً:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

*(يرجى تغيير كلمة المرور بعد الدخول الأول لضمان الأمان)*
